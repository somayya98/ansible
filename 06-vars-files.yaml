- name: variables from files
  hosts: localhost
  connection: localhost
  vars_files:
  - course.yml
  tasks:
  - name: print course info
    ansible.builtin.debug:
      msg: "Course Name is: {{ COURSE }}, Duration: {{ DURATION }}, {{ Trainer }}"